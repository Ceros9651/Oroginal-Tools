# OpenSpecとは何か？
>この記事は私の勉強兼知識のアウトプットを主目的とした記事です。ご留意ください。

OpenSpecは**コードを書く前に「何を作るか」を人間とAIで文書として合意し、それをファイルとしてリポジトリに残す**アプローチです。合意した仕様が残るので、セッションをまたいでも前提がぶれません。
OpenSpecはMarkdownファイル数枚で完結する点が軽量で、他のフレームワークと比べて扱いやすいのが特徴です。

この記事ではOpenSpecを使う上で必要な知識を習得しましょう。

## この記事で作るアプリケーション
この記事では、「筋トレ用カウントダウンタイマー」を作ります。
2分筋トレ→1分休憩→5分走る→2分休憩、というように自分で時間のループを決めることができるようなアプリをイメージしてください。

## OpenSpecの環境構築
OpenSpecハンズオンを始める前に環境構築をしておきましょう。
必要なのは、`Node.js`とGitHub CopilotやClaude CodeなどのAIコーディングツールです。本記事ではClaude Codeを採用しています。
### Node.jsのインストール
こちらの[Node.js公式サイト](https://nodejs.org/ja/download)からインストールしてください。

### OpenSpecのインストール
> 前提：Node.js 20.19.0 以上が必要です（`node -v` で確認）。

```bash
npm install -g @fission-ai/openspec@latest
```

インストールが終わったら、プロジェクトを配置したいフォルダでターミナルを開いて初期化します。

```bash
cd /path/to/your/dev-folder
openspec init
```

`openspec init` は対話式で、**どのAIツール向けの設定を生成するか**を聞かれます。
（Claude Code / Cursor / Copilot など25種類以上から選択）今回はClaude Codeを選びます。
完了すると、フォルダ内に `openspec/` ディレクトリと、AIツール用のスラッシュコマンド定義
（Claude Codeの場合 `.claude/` 配下）が生成されます。

これで環境構築は完了です。
なお、以降の `/opsx:xxx` は**ターミナルではなくAIチャット（Claude Code等）に入力する**コマンドです。スラッシュコマンドの候補に出てこない場合は、AIツールを再起動してみてください。

> ※ 本記事は OpenSpec v1.5.0 時点の出力です。バージョンによって生成されるセクション名が異なる場合があります。


## OpenSpecの流れ
最初に、流れのイメージが湧くようにどのような流れの開発なのかを説明します。

- `/opsx:propose`で`proposal`・`spec`・`design`・`tasks`を作る
- `/opsx:apply`でコード実装する
- `/opsx:archive`で、書いた仕様を確定版へ反映する（＋変更を履歴として保存する）

という手順を踏みます。各ステップをここから深く学んでいきましょう。

`/opsx:propose` を実行すると、以下の4つの成果物がまとめて生成されます。

- `proposal.md`（なぜ・何を変えるか）
- `specs/<機能名>/spec.md`（システムがどう振る舞うべきか）
- `design.md`（どう実現するか）
- `tasks.md`（実装チェックリスト）

本記事では理解しやすいよう、これらを別々のステップとして順に解説します。

### 1．`/opsx:propose`で`proposal.md`を作る

最初に、`/opsx:propose`というコマンドを使用して`proposal.md`と呼ばれるファイルを作ります。

この`proposal.md`は

**何を・なぜ変更するのか？を人間とAIの間で合意するためのドキュメント**

です。本変更での実装方針を決めるという役割を持ちます。

OpenSpecでは`proposal.md`が起点であり、以降の判断基準になります。以降のステップはすべて`proposal.md`を参照して進むため、開発品質を高めるためには`proposal.md`をいかに高品質に仕上げるかが重要です。

#### 開発の品質を上げるために

 `/opsx:propose`というコマンドで4つの成果物をまとめて生成してくれることは先ほどお伝えしましたが、私は`proposal.md`が生成された時点で`proposal.md`をレビューすることを強くお勧めします。
 `proposal.md`は開発の指針であるため自分の認識と齟齬がないかを確認しなければなりません。しかしながら、`proposal.md`を確認しないまま`design`と`tasks`を作ってしまうと認識が違ったまま実装方針が立ってしまい、成果物の手戻りにつながってしまう恐れがあります。

公式のデフォルトフロー（`core`プロファイル）では `/opsx:propose` が4つの成果物を一括生成しますが、拡張プロファイルを有効にすると `/opsx:new`（changeの器だけ作る）→ `/opsx:continue`（成果物を1つずつ進める）という段階実行ができます（`openspec config profile` で切り替え）。つまり「途中で止めてレビューする」こと自体は公式も想定した使い方です。
本記事では設定を増やさないため、チャットで「proposal.mdができたら止めて」とお願いする方法を採ります。
（※この方法はAIの判断次第なので、止まらずに4つ作られてしまうこともあります。その場合は`proposal.md`だけ先に読んで、気になる点をチャットで指摘すれば大丈夫です）

#### 実際に`proposal.md`を作ってみる

では実際に筋トレ用カウントダウンタイマーを作っていきましょう！

まずどんなアプリを作りたいか、どんな機能を持たせたいかをAIに教える必要があります。長文で書くよりかは機能ごとに箇条書きで教えてあげるのがよいです。後で詳細要件は擦り合わせることができるため、細かく正確に教えなければ！と肩に力を入れる必要はありません。
皆さんがイメージするカウントダウンタイマーの仕様を教えてあげれば大丈夫です。

以下のように、`/opsx:propose`というコマンドを使用し、かつ`proposal.md`を作った時点で止めるように指定して、AIチャットに送信しましょう。

**プロンプトの一例**

```
/opsx:propose

以下の仕様を持つアプリケーションのproposalを生成してください
・カウントダウンタイマー
・ユーザーが「作業○分→休憩○分」の組み合わせを好きな順番・好きな回数だけ登録できる
・時間の単位は分数
・タイマーの種類は「作業」と「休憩」の2種類にすること

proposalを確認したいので、proposal.mdができたら一時ストップして見せてください
```


### 2．`proposal.md`を確認・レビューする

`proposal.md`は下記セクションを持った構成です。
#### Why（背景と目的）

「**なぜこの変更を行うのか？**」を定義したものです。
変更を加えることとなった背景、根拠、この変更を行わない場合どのような不都合が起きるのか、を記載します。

今回のタイマーアプリの`proposal.md`の`Why`は以下のようになりました。

```md
集中して作業する時間と休憩する時間を交互に繰り返したいユーザーは多いが、固定的なポモドーロ（25分作業／5分休憩）だけでは各自の作業リズムに合わない。
ユーザーが「作業○分→休憩○分」の組み合わせを自由に設計し、その通りに自動で進行するカウントダウンタイマーが必要である。
```

#### What Changes（変更内容）

「**どこをどのように変更するか？**」を定義したものです。
ユーザーがやってほしいと言ったことを整理して、箇条書きで記載してくれます。

今回のタイマーアプリの`proposal.md`の`What Changes`は以下のようになりました。

```md
- カウントダウン方式のタイマーアプリを新規に追加する。
- ユーザーが「作業○分」「休憩○分」の区間（インターバル）を、**好きな順番・好きな回数**でリストとして登録できる。
- 各区間の時間は**分単位**で指定する。
- タイマーの種類は「**作業**」と「**休憩**」の2種類のみとする。
- 登録した区間リストを先頭から順に自動で進行し、1区間が0になると次の区間へ自動遷移する。区間の切り替わりはユーザーに通知する。
- タイマーの開始・一時停止・再開・リセットができる。
```

#### Capabilities

「**開発物に搭載する機能を機能単位で分割したもの**」です。
例えば、`認証機能`・`決済機能`・`コンテンツ管理機能`などの機能がここで箇条書きで明記されます。
それぞれの機能にかっこいい名前を付けて整理するのがこのセクションの役割です。

Capabilitiesには`New Capabilities`と`Modified Capabilities`の2つがあります。

- New Capabilities

既存のアプリにはなく、新しく作る機能のことです。

```md
- `interval-schedule`: ユーザーが「作業／休憩」の区間を任意の順番・回数で追加・並べ替え・削除し、実行するスケジュール（区間リスト）として管理する機能。
- `countdown-timer`: 登録されたスケジュールを先頭から順にカウントダウン実行し、区間の自動遷移・完了通知・開始／一時停止／再開／リセットを行う実行エンジン機能。
```
- Modified Capabilities

既存のアプリにある機能の中で変更する機能のことです。
今回は新規開発のため、該当なしです。

#### Impact
この機能追加によって想定される既存コードへの影響を示したものです。
今回だと以下のようになります。

```md
- 新規プロジェクトのため既存コードへの影響はなし。UI 層・タイマー状態管理ロジック・区間データモデルを新規に追加する。
- 依存関係やデプロイ構成は本 proposal の段階では未確定（design.md で決定する）。
```

---

以上が`proposal.md`の構成です。
`proposal.md`ができあがったらそれを人間がレビューします。

筆者の経験上、`proposal.md`を見たときに感じる違和感は以下のようなものです。

- この仕様は必要ないのではないか？
- これは私が思っていた仕様変更とは違う
- 明らかにこの仕様変更は過剰である・Too muchである
- 言葉の使い方がおかしい、自分の想像とは違う

AIくんは頭が賢すぎるため想像以上の`proposal.md`を作ってくれます。それが自分の認識とあっているかどうかは必ず人間が確認しないといけません。
もし修正してほしい点があれば、チャットで「～は今回の範囲外である」「～を～に修正して」というように再度AIに頼んで修正させましょう。
ここはとことん壁打ちをして、自分の認識とあっていて納得できるまで質を上げていきましょう。

### 3．`spec.md`を作成する

proposal のレビューが終わったら、次に `spec.md` を作成してもらいます。
と言っても、`proposal.md`を作り終わるとAIくんが「次に進んでいいなら続けて、と伝えて」という指示をしてくれるので、それに沿って対話を進めればOKです。
先出しになりますが、残りの生成物は`specs/<機能名>/spec.md`、`design.md`、`tasks.md`の3つです。これらはまとめて作ってしまって問題ないため、特に制限せず「続けて」とAIに送って構いません。

`spec.md` とは「**What：システムがどう振る舞うべきか**」を定義したファイルです。`proposal.md` が「なぜ・何を変えるか」の合意だとすれば、`spec.md` はそれを**検証可能な要件の形に落とし込んだもの**です。

ここで最も大事なルールは、**`spec.md` には実装の話を書かない**ということです。
「React の useState で管理する」「setInterval を使う」といった話は `design.md` の担当です。`spec.md` に書くのはあくまで「ユーザーから見て何が起きるか」だけです。

#### ファイルの置き場所

`spec.md` は proposal の `Capabilities` と 1 対 1 で対応します。今回は `interval-schedule` と `countdown-timer` の 2 つの機能を定義したので、ファイルも 2 つできます。

```
openspec/changes/add-interval-timer/
├── proposal.md
├── design.md
├── tasks.md
└── specs/
    ├── interval-schedule/
    │   └── spec.md
    └── countdown-timer/
        └── spec.md
```

proposal で機能名を整理しておくと、ここで自然にファイル分割されるというわけです。Capabilities に「かっこいい名前を付ける」意味がここで効いてきます。

#### デルタ（差分）で書く

OpenSpec の `spec.md` の最大の特徴は、**現状からの差分（デルタ）として書く**ことです。「完成形の仕様書」ではなく「今回の変更で何が増え、何が変わり、何が消えるか」を書きます。

そのため見出しは次の 3 種類になります。

- `## ADDED Requirements` … 今回追加する要件
- `## MODIFIED Requirements` … 既存の要件を変更する場合（変更後の全文を書く）
- `## REMOVED Requirements` … 削除する要件

今回は新規開発なので `ADDED` のみになります。

#### 要件とシナリオの書き方

各要件は「Requirement（要件）」と、それを検証する「Scenario（シナリオ）」の組で書きます。

- Requirement は `SHALL` / `MUST` を使い、システムを主語にした 1 文で書く
- Scenario は `GIVEN`（前提）→ `WHEN`（操作）→ `THEN`（結果）の形式で書く
    - ただし GIVENは前提条件が自明な場合は省略され、WHEN / THEN だけになることも多い
- **1 つの Requirement には最低 1 つの Scenario が必要**

実際に生成された `countdown-timer` の `spec.md` は以下のようになりました。

**`specs/countdown-timer/spec.md`**

```md
## ADDED Requirements

### Requirement: カウントダウン実行
システムは実行中の区間について、指定された分数を起点に残り時間を減らしていくカウントダウンを行わなければならない（SHALL）。残り時間は少なくとも秒単位で表示しなければならない（SHALL）。

#### Scenario: 区間のカウントダウンを開始する
- **WHEN** ユーザーが区間リストのタイマーを開始する
- **THEN** システムは先頭の区間を現在区間とし、その分数を秒に換算した残り時間から減算を開始する

#### Scenario: 残り時間の表示
- **WHEN** タイマーが実行中である
- **THEN** システムは現在区間の種類（作業／休憩）と残り時間（分：秒）を表示する

### Requirement: 区間の自動遷移
システムは現在区間の残り時間が0になったとき、リスト上の次の区間へ自動的に遷移しなければならない（SHALL）。

#### Scenario: 次の区間へ進む
- **WHEN** 現在区間の残り時間が0に到達し、リストに後続の区間が存在する
- **THEN** システムは次の区間を現在区間とし、その分数からカウントダウンを継続する

#### Scenario: 区間切り替わりの通知
- **WHEN** 区間が別の区間へ切り替わる
- **THEN** システムはユーザーへ切り替わりを通知する

### Requirement: 全区間の完了
システムは最後の区間の残り時間が0になったとき、スケジュール全体の完了として実行を終了しなければならない（SHALL）。

#### Scenario: スケジュール完了
- **WHEN** 最後の区間の残り時間が0に到達する
- **THEN** システムは実行を停止し、全区間の完了をユーザーへ通知する

### Requirement: タイマーの操作
システムはタイマーの開始・一時停止・再開・リセットを行えるようにしなければならない（SHALL）。

#### Scenario: 一時停止する
- **WHEN** 実行中にユーザーが一時停止を指示する
- **THEN** システムはカウントダウンを停止し、現在区間と残り時間を保持する

#### Scenario: 再開する
- **WHEN** 一時停止中にユーザーが再開を指示する
- **THEN** システムは保持した残り時間からカウントダウンを再開する

#### Scenario: リセットする
- **WHEN** ユーザーがリセットを指示する
- **THEN** システムは実行を停止し、現在区間を先頭に戻して残り時間を各区間の初期値に戻す
```
`spec.md`は`proposal.md`ほど時間をかける必要はありません。AIが構成を間違えることはほとんどなく、内容もレビューしたところで経験的に「へぇーー、いいんじゃね」で終わるのがほとんどだからです。
ただし後述しますが、**`spec.md`はアーカイブ時に確定版の仕様として残り続けます**。「Scenarioに書かれた振る舞いが、自分の意図と食い違っていないか」だけはざっと確認しておくと、半年後の自分が助かります。

### 4．`design.md`をレビューする
`design.md`というファイルもできているはずですので、次は`design.md`をレビューしましょう。
（作られていない場合は、その変更が設計判断を要さないほど単純だったということです）
`design.md`は「**How：どのように`proposal.md`の内容を実現するか**」を定義する設計書です。具体的には、アーキテクチャ、採用技術、検討した代替案とトレードオフ、データモデル、移行手順、未解決の論点などが記述されます。

#### `design.md`の構成
##### Context（前提・制約）

「この設計を考えるにあたって、何が決まっていて何が決まっていないか」を書きます。
既存コードの状況、技術的な制約、そして「この変更で一番の難所はどこか」を明らかにするセクションです。後述の例では「コア難所」として明記されています。

```md
## Context

新規プロジェクト（Original-Timer）で、区間タイマーをゼロから実装する。既存コード・既存 spec はなく、依存関係やスタックの制約も未確定。要求は proposal.md と 2 つの spec（`interval-schedule` / `countdown-timer`）で定義済み。

主な性質:
- 単一ユーザーがローカルで使うツール。サーバー側処理・認証・複数ユーザー同期は不要。
- コア難所は「正確な残り時間のカウントダウン」と「区間の自動遷移」という状態遷移ロジック。
- UI は「区間リストの編集」と「実行画面（残り時間・現在種別・操作）」の 2 面。
```

##### Goals / Non-Goals（やること・やらないこと）

「この設計が達成すべきこと」と「あえて達成しないこと」を書きます。

重要なのは **Non-Goals** のほうです。AI は放っておくと親切心で機能を盛りにいきます。ここで「やらない」と明文化しておくことが、スコープの暴走を防ぐブレーキになります。`proposal.md` の段階で「今回の範囲外」と伝えた内容が、ちゃんとここに落ちているかをレビューするとよいでしょう。

```md
## Goals / Non-Goals

**Goals:**
- 追加依存・ビルド不要で動く、自己完結した単一ページのタイマーを実装する。
- 区間リストの追加／並べ替え／削除と、開始・一時停止・再開・リセットを提供する。
- 分単位で登録し、残り時間は「分：秒」で表示する。区間 0 で自動遷移＋通知する。
- タイマー計時が経過実時間ベースで正確であること（後述の drift 対策）。

**Non-Goals:**
- 複数スケジュールの保存・切り替え、クラウド同期、アカウント管理。
- サーバーバックエンド、モバイルネイティブアプリ。
- 秒単位・時間単位での区間指定（単位は分に固定）。
```

##### Decisions（技術的な決定）
`design.md` の心臓部です。「何を決めたか」だけでなく、「なぜそう決めたか」「何と比較して選んだか」まで書くのがポイントです。この`Decisions`で設計者とAIの齟齬が起きる可能性が高いです。
- この機能は意図の通りではない
- この変数Aを変えるといっているけど影響が大きいからあまり変更してほしくない
- この機能は明らかに不要 or 余分である

などがレビューでの指摘点です。よく見ておきましょう。

```md
## Decisions

### 決定1: スタックは依存ゼロのバニラ Web（HTML + CSS + JavaScript）
単一ファイル群（`index.html` / `style.css` / `app.js`）で完結させ、ブラウザで開くだけで動作させる。
- **理由**: タイマーはロジックが小さく、フレームワークのビルド基盤はオーバースペック。依存ゼロなら Windows 環境でセットアップ不要、レビューも容易。
- **代替案**: React/Vite（コンポーネント分割は綺麗だがビルド・依存が増える）→ 規模に見合わず却下。Electron（デスクトップ化できるが配布が重い）→ Non-Goal のため却下。将来フレームワーク化する場合もロジックを純粋関数に寄せておけば移行しやすい。

### 決定2: 計時は「終了時刻ベース」で行い、`setInterval` は表示更新のみに使う
残り時間を「毎 tick で 1 秒引く」のではなく、開始時に `endTime = now + 残りミリ秒` を保持し、表示更新時に `remaining = endTime - now` を再計算する。表示更新は `setInterval`（約 250ms）で行う。
- **理由**: `setInterval` はタブ非アクティブ時などに間引かれ、単純減算では時間がずれる（drift）。実時間から算出すれば累積誤差が出ない。
- **代替案**: 単純な 1 秒 `setInterval` 減算 → 実装は簡単だが精度要求（Goals）を満たせないため却下。

### 決定3: タイマー状態を有限状態機械（FSM）として明示する
状態を `idle` / `running` / `paused` / `completed` の 4 つとし、遷移（start / pause / resume / reset / tick-zero）を 1 箇所で管理する。現在区間は「区間リスト内のインデックス」で表す。
- **理由**: 一時停止・再開・自動遷移・完了が絡む挙動はバグの温床。状態と遷移を明示すると spec のシナリオと 1:1 で対応づけてテストしやすい。
- **一時停止の扱い**: pause 時は `endTime` から残りミリ秒を確定して保存し、resume 時に `endTime` を再計算する。

### 決定4: データモデル
- `Interval = { id: string, type: '作業' | '休憩', minutes: number }`（`minutes` は 1 以上の整数）。
- `Schedule = Interval[]`（順番そのものが実行順）。
- 実行状態 = `{ status, currentIndex, endTime | remainingMs }`。

### 決定5: 永続化は localStorage（任意・軽量）
編集した区間リストを `localStorage` に保存し、リロード後も復元する。実行中の状態は永続化しない。
- **理由**: サーバー不要でユーザー体験が向上する。Non-Goal の「複数スケジュール管理」には踏み込まず、単一リストのみ保持する。

### 決定6: 通知は画面表示＋任意の音
区間切り替わり・全完了は、画面上の明示的な表示（現在種別の切り替え・完了メッセージ）で通知する。加えて短い効果音（Web Audio）を鳴らす。ブラウザ通知 API は許可が必要なため任意扱いとする。
```

##### Risks / Trade-offs
「この設計を選んだことで発生する弱点と、その対処」を書くセクションです。
想定される弱点 → それに対する対処方法という流れで書かれていることを確認し、内容に問題がないかをレビューします。

```md
## Risks / Trade-offs

- [タブ非アクティブ時にタイマー音や描画が遅延する] → 計時は終了時刻ベースなので残り時間の正確性は保たれる。再アクティブ化時に正しい残り時間へ即再描画する。
- [依存ゼロ構成ゆえテスト基盤も手薄になりがち] → コア計時・FSM・区間遷移を副作用のない純粋関数に切り出し、軽量なテスト（あるいは手動シナリオ）で spec のシナリオを検証可能にする。
- [localStorage が使えない／無効な環境] → 保存失敗時は握りつぶし、メモリ上のリストで動作継続する（機能低下のみ）。
- [多数の区間を登録した場合の UI 煩雑化] → 本リリースはシンプルなリスト UI に留め、グルーピングやテンプレートは将来課題とする。
```
##### Open Questions
「未確定要素」を書くセクションです。
AIが決めきれなかった要素を記述し、開発者に指示を仰ぐものです。基本的な構成としては、未確定要素 → 初期案となっており、初期案でよければ放置、何か指定したいものがあればチャットで指示を出しましょう。

```md
## Open Questions

- 効果音は同梱アセットにするか Web Audio の合成音にするか（配布容易さから合成音を第一候補とする）。
- 並べ替え UI はドラッグ&ドロップか上下ボタンか（実装容易さから上下ボタンを初期案とする）。
```
これらの構成で`design.md`は記述されます。
`design.md`はちゃんとレビューしておきましょう。これが成果物の方針の詳細決定版になるため、認識齟齬が起きてしまうと成果物の質に悪影響を及ぼしてしまいます。特に、`Goals / Non-Goals（やること・やらないこと）`が重要です。チケットの範疇を超えていないか、不足はないか、などで齟齬が起きることが多いため見ておきましょう。



### 5．`tasks.md`をかるーくレビューする

OpenSpecの成果物の中で最後に作られる実装のチェックリストが`tasks.md`で、チェックボックス付きのMarkdownテキストとして生成されます。
**`tasks.md`**

```md
## 1. プロジェクト土台

- [ ] 1.1 `index.html` / `style.css` / `app.js` の空ファイルを作成し、ブラウザで開いて読み込まれることを確認する
- [ ] 1.2 画面を「区間リスト編集エリア」と「タイマー実行エリア」の2領域にレイアウトする

## 2. データモデルと区間スケジュール管理（capability: interval-schedule）

- [ ] 2.1 `Interval = { id, type: '作業'|'休憩', minutes }` と `Schedule = Interval[]` のデータ構造を定義する
- [ ] 2.2 区間追加関数を実装する（種類は「作業」「休憩」の2択のみ、それ以外は生成不可）
- [ ] 2.3 分数入力のバリデーションを実装する（1以上の整数のみ許可し、0・負数・非整数は拒否）
- [ ] 2.4 区間の並べ替え（上下移動）と削除を実装する
- [ ] 2.5 区間リスト UI を実装する（種類・分数の表示、追加フォーム、並べ替え／削除ボタン）
- [ ] 2.6 リストを `localStorage` に保存・復元する（保存失敗時はメモリ動作にフォールバック）

## 3. カウントダウン実行エンジン（capability: countdown-timer）

- [ ] 3.1 状態 `idle`/`running`/`paused`/`completed` と現在区間インデックスを持つ FSM を実装する
- [ ] 3.2 終了時刻ベースの計時（`endTime = now + 残りms`、残り = `endTime - now`）を実装する
- [ ] 3.3 約250msの表示更新ループで残り時間を再計算し、「分：秒」と現在種別を描画する
- [ ] 3.4 区間の残り時間が0に到達した時に次区間へ自動遷移する処理を実装する（後続がある場合）
- [ ] 3.5 最後の区間の残り時間が0に到達した時に全完了として実行終了する処理を実装する
- [ ] 3.6 開始・一時停止・再開・リセットの操作を実装する（pause で残りms確定、resume で endTime 再計算、reset で先頭・初期値に戻す）
- [ ] 3.7 空リスト（0件）での開始を禁止し、ユーザーへ促すメッセージを表示する

## 4. 通知

- [ ] 4.1 区間切り替わり時の画面通知（現在種別の切り替え表示）を実装する
- [ ] 4.2 全区間完了時の完了メッセージ表示を実装する
- [ ] 4.3 区間切り替わり・完了時の効果音（Web Audio 合成音）を鳴らす

## 5. 検証

- [ ] 5.1 計時・FSM・区間遷移のコアロジックを副作用のない純粋関数に切り出す
- [ ] 5.2 `interval-schedule` spec の各シナリオを手動またはテストで検証する
- [ ] 5.3 `countdown-timer` spec の各シナリオ（自動遷移・一時停止／再開・リセット・完了）を手動またはテストで検証する
- [ ] 5.4 タブ非アクティブ→復帰後に残り時間が実時間とずれていないことを確認する

```
#### 役割
`design.md`で決まった「なぜ・なにを・どのように」を上から順に実行できる作業単位に分割して整理したものと捉えておきましょう。
AIくんは1から順番に実装を進めてくれます。この`tasks.md`をもとに、AIが実際にコードを書く実装フェーズへ入ります。

#### レビュー
正直、`tasks.md`をまじめにレビューする必要はありません。なぜかというとこれまでで決定されてきた方針をそのまま実装に落とし込んだだけで特段新しい内容はないためです。
ただ、念のため上から流し読みする程度で見ておきましょう。

### 6．コード実装をする
実装をしてもらうためには、`/opsx:apply`というスラッシュコマンドを使用します。
以下のようにチャットで指示を送りましょう。
```
/opsx:apply
tasks.mdを実装して
```
これを送るとAIくんが黙々とコードを生成してくれます。片手間に見ておくといいです。
実行の流れとしては、

1. `tasks.md`のチェックボックスを順に埋めていきながらコーディングを進める
2. コーディングが終われば動作テストをする
3. 動作テストが終われば「実装が終わったよ」と言ってくれる

という流れです。

### 7．成果物を見てみる
私の場合だと、AIくんが作ってくれたタイマー（`index.html`）はこのようなものになりました。
![](https://static.zenn.studio/user-upload/ae1ba835abf1-20260723.png)
なかなかいいものができましたね！シンプルかつ挙動も問題なさそうです。

自分が作ったものに納得がいかない人はAIくんに指摘して修正を繰り返してもらいましょう。AI君は心が寛大なのでどんだけ文句言っても対応してくれます。

#### 現状を確認するCLIコマンド

「今どうなってるんだっけ」と迷ったときは、ターミナルで以下のコマンドを叩くと状況を確認できます。

```bash
openspec list                        # アクティブなchangeの一覧
openspec show add-interval-timer     # changeの詳細
openspec validate add-interval-timer # spec記法の検証
openspec view                        # 対話的ダッシュボード
```

### 8．アーカイブして仕様を確定させる

実装が終わったら、最後に `/opsx:archive` を実行します。

`/opsx:archive` は「終わった変更をフォルダに片づけるコマンド」ではありません。
**今回の変更で書いた仕様を、これまで積み上げてきた確定版の仕様へマージする**コマンドです。

OpenSpec はプロジェクトの仕様を2層で管理しています。

- `openspec/specs/` … 現状のシステムがどう動くかを記述した確定版の仕様。過去の変更が累積されています
- `openspec/changes/<変更名>/` … 確定版に対する変更の提案。まだ確定していません

今回の開発で書いた `specs/` 配下の `spec.md`は、確定版そのものではなく、確定版からの変更点・新しく追加する仕様です。セクション3でも説明しましたが変更点や追加仕様を差分（デルタ）と呼びます。この差分を確定版へ反映させること、これがアーカイブの役目です。

逆に言えば、アーカイブしない限り確定版は一行も更新されません。コードには新機能が入っているのに、仕様書だけ実装前の姿で取り残された状態になります。この状態で次の開発を始めると、AI は古い仕様を読んで提案を作ります。「さっき実装したはずの機能が存在しない前提」で話が進んでしまうわけです。アーカイブは必ず行いましょう。

意味が分かったところで、実行してみましょう。

```
/opsx:archive
```

AIが自動で進めてくれるので、待っていればOKです。
完了すると `openspec/changes/archive/2026-07-23-add-interval-timer/` のような日付付きフォルダができ、その中に開発で生成した`proposal.md`や`tasks.md`などが格納されています。
同時に `openspec/specs/` が更新されていることも確認してみてください。ここまでの説明が、自分の手元で実際に起きていることが分かるはずです。

## 成果物をアウトプット後のフォルダ構成を見る
一連の開発フローが終わりました。ここで、フォルダ構成を見てみましょう。
```
Original-Timer/                                  # プロジェクトルート
├── index.html                                   # 画面の骨組み。編集エリア/実行エリアの2領域
├── style.css                                    # 全スタイル。ダークテーマ、作業=青/休憩=緑
├── core.js                                      # コアロジック。DOM非依存の純粋関数のみ
├── app.js                                       # UI層。DOM描画・localStorage・Web Audio
├── README.md                                    # プロジェクト説明
├── test/
│   └── core.test.js                             # spec準拠テスト22件（node単体で実行可）
├── openspec/                                    # 仕様管理
│   ├── config.yaml                              # OpenSpec設定
│   ├── specs/                                   # ★現在の正となる仕様
│   │   ├── interval-schedule/
│   │   │   └── spec.md                          # 区間管理の要求 5件/11シナリオ
│   │   └── countdown-timer/
│   │       └── spec.md                          # 実行エンジンの要求 4件/9シナリオ
│   └── changes/                                 # 変更提案の置き場（現在アクティブ0件）
│       └── archive/                             # 完了済みchangeの履歴
│           └── 2026-07-23-add-interval-timer/   # 今回の変更一式
│               ├── proposal.md                  # なぜ作るか・何を変えるか
│               ├── design.md                    # 技術判断と、その理由
│               ├── tasks.md                     # 実装チェックリスト 22/22完了
│               └── specs/                       # デルタspec（上のspecs/へ同期済み）
│                   ├── interval-schedule/
│                   │   └── spec.md
│                   └── countdown-timer/
│                       └── spec.md
└── .claude/                                     # Claude Code設定（既存・今回未変更）
    ├── commands/opsx/                           # /opsx:* スラッシュコマンド 5種
    └── skills/                                  # openspec-* スキル 5種

```

## 最後に
ハンズオンお疲れさまでした！
今回は筋トレ用アプリを作成しましたが、作成するものは何でも構いません。自分が作りたいものを作ってみてください。
私はフロントエンドやバックエンドの知識はあまりありませんが、このレベルのアプリを作れてしまう時代がもう来ているのですね、今後の進歩にも期待です。
ありがとうございました！
